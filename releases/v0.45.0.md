# v0.45.0 — 2026-07-19

## Changed

- Framework: /wave:\* trains + archival lifecycle — pipeline hierarchy grew a level (trains of waves of tasks): `docs/dev/trains/{train-name}/` is the home for every train-scoped ledger (train-goal.md, lanes.md, pool.md, decisions.md, train-notes.md, ad-hoc checkpoints — previously flat unarchivable files under docs/dev/waves/); `/wave:schedule` S4 seeds the dir, orchestrator/watcher write it, orchestrator § O6.7 archives it whole + every consumed queue spec (commit → move to tmp/dev/archive/{trains,waves/queue}/ → commit, gitter DOCS-COMMIT `Archive:` — entries may now be files, not only dirs); watcher round gains an archival-squatter audit (shipped wave dirs / dead train dirs / consumed specs still under docs/ = named defect); `/wave:live` W8 archives a consumed queue spec with its wave; parked wave packages re-home from `docs/dev/waves/.waiting/` to `docs/dev/backlog/waves/` and the feature backlog to `docs/dev/backlog/backlog.md` (documenter registry/scope/sync-rules re-pointed); command research output (officer/pm/marketer) re-homes from `docs/dev/research/` to `.professor/RR/` (gitignored — git history keeps committed records) and the squatter audit + O6.7 sweep cover `docs/dev/builds/` too.

- Framework: AskUserQuestion founder-visibility law — the founder sees only the dialogs (chat prose between them never renders for him): all context travels inside the question text; a clarification answer opens the NEXT question's title, simpler and more concrete each round, never a rephrase (wave/refine.md § Founder interaction + root CLAUDE.md § Meta).

- Framework: descriptions-as-router law — /pcm § Authoring conventions gains "Descriptions — the routing registry" (a description names every user-nameable subcommand/mode/flag/alias with its trigger form — an unnamed sub-functionality is unroutable; every clause routes or instructs; compact) + enforcement rows in the commands/skills audit scopes; registry-wide sweep applied it to every command, skill, and workflow meta — 28 descriptions gained missing entry points (orchestrator resume forms; /pcm `retro` + `audit [scope]`; schedule `rebalance`; walker investigate/panel modes; dev full mode router + corrected argument-hint; jc `deploy`/diagnostics/`boundary-lite`; quality:prompt `cut`; quality:doc approval gate; pcm:update `check`/`--to`/`--force`/`--re-interview`; context-meter `--verbose`; p:tokens flags; 360/rnd/slow-burn trigger forms; inject 🔖/`--force-now`/`--file`/`--then`; read sid/jsonl path; marketer/mentor/officer/pm/km/documenter mode lists; rr midrun/resume; legal DPA-drafting + skill-optimizer; audit-cortex-sessions `args.channel`; documenter-fanout mode args) and shed noise clauses (security sacred-flourish, qa:live chain recital, refine stage recital, 360 philosophy tail, ghostwriter restatement, branch tmux trivia, new four-eye label, h:gh restatement).

- Framework: stale `wave-build.js` references cut — /pcm wiring diagram + invariant 12 sync-set now name only the three live workflows (documenter-fanout marked canonical, documenter.md § Orchestration the pointer); documenter-fanout.js DOC_BRIEF sync comment corrected to single-copy; wave-walker.js meta gains the Territory Digests step the script implements (declared-copy accuracy vs wave/walker.md § Orchestration).

- Improve: wave/orchestrator.md — boundary-lock holder lines carry the stamping session sid + ask-a-reachable-owner-before-takeover; unowned "left for task #N" defers rejected at verdict; proceed-span verdicts restate the hold-writes-until-checkpoint law; accept-then-checkpoint as one atomic act; idle-lane holders triaged per item against the specific contended resource (a window is never a blanket holder); merge-SHA return = dispatch event for merge-held /jc items.

- Improve: wave/builder.md — hand briefs carry ABSOLUTE report paths; fix-loop "pre-existing" claims require main-comparison proof else the hand fixes it; FE hands npx-only (pnpm invents a foreign lockfile in an npm project).

- Skills: rr → v3.2.2, ghost-writer → v1.4.2 (description-router sweep applied to both). **`update`: skip — informational only** — these are source-fetched (`sources.json`), never vendored in the blueprint; a fresh install or `/pcm update` re-pulls the latest tag from each skill's own repo.

## Fixed

- Fix: wave-wait.sh — exit-trap compare-and-delete (rm wait.pid only when its content == $$); a replaced waiter's delayed trap no longer deletes the successor's pid file.

- Fix: statusline cache-window mtime — dual `stat` (BSD `-f%m` / GNU `-c%Y`) + zero-mtime guard; the segment is skipped when the transcript mtime is unreadable (GNU-only `stat -c%Y` fails silently on macOS, and the `0` fallback was rendering a bogus `✗495665h`).

## Migration

- adopters with running trains — finish the live train on the old paths, then re-home; `mkdir -p docs/dev/trains docs/dev/backlog/waves` and move any flat lanes/pool/decisions/train-goal ledgers into `docs/dev/trains/{train-name}/`; move `.waiting` packages to `docs/dev/backlog/waves/`.
