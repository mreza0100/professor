# v0.24.0 — 2026-06-11

Token-optimization campaign: persona relocated to output styles, Analysis Protocol folded into persona (`p:analysis` removed), `/audit` removed, routing-gated `/build` + common spawn contract + consolidated 6-bugs, delta STATE.md waves, gitter/build/refine trims, QA promoted to registered hook-carrier wrappers with test-output filter, rr collection/judgment split, prompt-law rails.

## Added

- Tier A: `templates/output-styles/professor.md` — Professor persona extracted from `CLAUDE.md` into a Claude Code output style (`keep-coding-instructions: true`); activates via `settings.json` `"outputStyle": "The Professor"`; main loop keeps the persona, ~33 subagents per build stop paying ~2.5k tokens of persona they never receive. Includes the cross-disciplinary Analysis Protocol (formerly `p:analysis`).
- Tier A: `templates/output-styles/jc.md` — JC persona extracted into its own overlay file; `/jc` reads and adopts it at invocation.
- Tier A: `templates/output-styles/dr-house.md` — Dr. House persona extracted into its own overlay file; `/pcm` reads and adopts it at invocation.
- Mechanics: `templates/scripts/filter-test-output.sh` — failure-biased PostToolUse filter for test-runner output (`{PACKAGE_MANAGER} test`/vitest/pytest/`{TEST_RUNNER}`); reads `tool_output`/`tool_response` defensively; emits `updatedToolOutput` via jq.
- Mechanics: `templates/agents/qa-wrapper.md` — template for `qa-{project}` registered root agents; thin wrappers (~650 B each) carrying PostToolUse/Bash hooks running `filter-test-output.sh`; hook path parameterized via `{REPO_ROOT}` placeholder; full protocol stays in `{project}/.claude/agents/qa.md`.
- Mechanics: `templates/docs-commands/gitter-history.md` — reference doc for all 6 gitter phase confirmation templates, ports.md template, and pre-migration history + large-file registry (moved out of gitter.md body).
- Mechanics: `templates/docs-commands/build-reference.md` — Pipeline step map, Step 0a stale-cleanup mechanics, BLOCKED.md template, and audit trail protocol (moved out of build.md body).

#### For: `templates/output-styles/`

Create `.claude/output-styles/` in your install. Add `professor.md` (the session persona), plus any per-command overlay files your install uses (e.g. `jc.md`, `dr-house.md`). Add `"outputStyle": "The Professor"` (or your renamed character) to `.claude/settings.json`. Commands that want a persona overlay add a one-line adopt pointer at invocation: `**Persona:** Read '.claude/output-styles/dr-house.md' now and adopt it`.

#### For: `filter-test-output.sh` + `qa-wrapper.md`

1. Copy `filter-test-output.sh` to `.claude/scripts/` and `chmod +x` it.
2. Replace `{PACKAGE_MANAGER}`, `{TEST_RUNNER}`, and `{REPO_ROOT}` placeholders with your values.
3. For each roster project, create `.claude/agents/qa-{project}.md` from the wrapper template, replacing `{project}`, `{PROJECT_ROLE}`, and `{REPO_ROOT}`.
4. Update `build.md` Steps 7 + 9 to spawn `Agent(qa-{project})` registered types (add transitional fallback clause for in-flight sessions: `if registry predates wrappers → spawn general-purpose + read child protocol`).

**`update`: steps 3–4 require your roster projects; no auto-apply.**

#### For: `gitter-history.md`

Create `docs/commands/git/references/gitter-history.md` from the template. In `gitter.md`, replace the inline confirmation-template table and ports.md template with one-line pointers to the new file.

#### For: `build-reference.md`

Create `docs/commands/build/references/build-reference.md` from the template. Replace `{project}` placeholders with your roster's project suffixes.

## Changed

- Tier A: `templates/CLAUDE.md` — dropped "Format all markdown" rule (PostToolUse format hook in `settings.json` enforces it deterministically); Analysis Protocol routing repointed to the active persona; Agents section adds `qa-{project}` hook-carrier wrapper pattern; `p:analysis` skill reference removed.
- Tier A: `templates/commands/build.md` — Step 1a planner fan-out is routing-gated (parse `**Routing:**` from 0-task.md, spawn only declared projects' planners, fall back to all roster on absence); repeated spawn boilerplate hoisted into one Common spawn contract; per-role doc-read lists replace "read ALL docs in $DOCS/"; Pipeline Reference table moved to `docs/commands/build/references/build-reference.md`; Steps 7 + 9 spawn `Agent(qa-{project})` registered types; QA findings write into consolidated `$DOCS/6-bugs.md`under per-project`## {PROJECT}`sections (per-project`6-bugs-{proj}.md` files eliminated); model tiers pointer added (`docs/commands/pcm/references/agent-models.md`).
- Tier A: `templates/commands/wave.md` — autonomous-execution contract reduced to "Same contract as /build" + wave-specific deltas; 0-task carve sharpened (each pipeline's 0-task.md carries ONLY its exact task slice + thin shared-rules header, no cross-pipeline bodies); STATE.md is delta-structured (rewritten resume brief + `## Next` on top, `<!-- APPEND-ONLY BELOW -->` marker, append-only adjudication archive below); model tiers pointer added.
- Tier A: `templates/commands/pcm.md` — 3 new critical invariants (#9 frontmatter registration = qa-{proj} thin-wrapper pattern; #10 registries read at session start = transitional fallback clauses; #11 voice placement in output-styles); persona inlined → one-line adopt pointer; `output-styles/` added to system wiring diagram and What you own table; qa-{proj} wrappers added to agent inventory; spawn-economics + @-imports facts folded into token-consciousness invariant; 7 new pipeline/spawn-design rules; mechanical-vs-semantic row tiering added to fan-out audit execution model.
- Tier A: `templates/commands/documenter.md` — AUDIT Step 1 mechanical existence checks MAY run as an `Explore` child; all DRIFT/STALE/contradiction judgment stays with the documenter.
- Tier A: `templates/skills/p:refine/SKILL.md` — detail-quality bar collapsed to one line; Task-11 anecdote deleted (rule lives in the reconciliation table); three POC-vs paragraphs collapsed to one sentence; architecture cluster gained read guard matching the api cluster.
- Mechanics: `templates/agents/gitter.md` — trimmed 20,571 → 15,330 B (under 15KB agent cap): pre-migration history, large-file registry, confirmation templates, and ports.md template moved to `gitter-history.md`; push-boundary and general rules deduped; `effort: high` added.
- Mechanics: `templates/agents/mono-documenter.md` — `effort: medium` frontmatter added.
- Mechanics: Skills/rr — Mode 1 rewritten from Workflow-JS script execution to dispatching ONE background `general-purpose` research agent (scout → fan-out → adversarial verify → synthesize); fan-out now splits collection from judgment: retrieval agents run as `model: "haiku"`/Explore collectors returning raw excerpts + sources only, with planning/verify/synthesize and all compliance/clinical retrieval held at the research-agent tier.

#### For: `build.md` — routing-gated fan-out

In Step 1a, update the planner spawn to check `**Routing:**` in 0-task.md before fanning out. Spawn only declared projects' planners. Add fallback: if no `**Routing:**` declaration, fan out to all roster projects.

**`update`: auto-apply mechanics; show diff for spawn-block changes.**

#### For: `build.md` — Common spawn contract

Extract the repeated per-spawn preamble (git ban, worktree-write ban, `$DOCS_REL` resolution, ZERO-GAP, doc-awareness) into a single `## Common spawn contract` section. Update each spawn block to reference it rather than repeat it.

**`update`: auto-apply mechanics.**

#### For: `build.md` — consolidated 6-bugs.md

Replace per-project `6-bugs-{project}.md` references with a single `$DOCS/6-bugs.md`. QA agents append findings under `## {PROJECT}` sections. Update consolidation step to verify section presence. Remove per-project `6-bugs-{proj}.md` files from pipeline artifact lists.

**`update`: auto-apply mechanics.**

#### For: `wave.md` — STATE.md delta structure

Rewrite the STATE.md update instructions: TOP is rewritten on each update (resume brief + `## Next`). Add `<!-- APPEND-ONLY BELOW — never rewrite -->` marker. Below the marker: append-only adjudication/decision archive.

**`update`: auto-apply mechanics.**

#### For: `p:refine/SKILL.md`

Auto-apply the trim.

**`update`: safe-auto.**

## Removed

- Tier A: `templates/commands/audit.md` — removed; `p:audit:code-hygiene` and `p:audit:security` skills are invoked directly, each carrying its own 360-sweep pre-step.
- Tier A: `templates/skills/p:analysis/` — removed; Analysis Protocol folded into `templates/output-styles/professor.md`.

#### For: `commands/audit.md`

Delete `.claude/commands/audit.md` from your install. Update any references in CLAUDE.md "Development Workflow" to invoke `p:audit:code-hygiene` / `p:audit:security` skills directly.

**`update`: walk through interactively — never auto-delete.**

#### For: `skills/p:analysis/`

Delete `.claude/skills/p:analysis/` from your install. In your `professor.md` output style (or `CLAUDE.md` if you haven't migrated), add the Analysis Protocol. Update CLAUDE.md "Cross-Disciplinary System Analysis" to point to the persona.

**`update`: walk through interactively — never auto-delete.**
