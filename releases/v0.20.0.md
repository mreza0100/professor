# v0.20.0 — 2026-06-10

## Changed

- Tier B: `p:refine` — RND-sourced waves carry the validated prompts verbatim. § R-POC output now applies to ANY wave built from RND work (in-flow POCs or a pre-existing `RND/` run the tasks originate from); refinement studies the RND output itself — the result, the iteration trail, and the exact prompts it converged on — and embeds each validated prompt in wave.md EXACTLY as-is: full text, byte-identical, in a fenced block, with the reason that wording works (what failed without it). A rewritten prompt is an unvalidated prompt — build agents reconstruct nothing. **`update`: safe-auto — sharpens an existing section, no structural change.**
