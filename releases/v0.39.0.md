# v0.39.0 — 2026-06-29

`/quality:prompt` becomes the mandatory first step before *every* prompt-file edit — not only when `/pcm` or `/km` loads it. Hand-editing a CLAUDE.md, agent, command, or skill now carries the same load-first obligation as a routed change.

## Changed

- **`templates/commands/quality/prompt.md` — mandatory for every prompt edit.** The `description:` now states `/quality:prompt` is the mandatory first step before any prompt-file edit, whether hand-edited or routed through `/pcm` or `/km` (previously: "Mandatory load for /pcm and /km"). The body already instructed loading it before hand-editing; the description now makes that obligation discoverable at routing time, so the rule isn't silently skipped on a quick hand-edit.
