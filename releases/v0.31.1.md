# v0.31.1 — 2026-06-16

A targeted fix to chat: injection — a message that is itself a slash command now lands verbatim so the target runs it.

## Fixed

- **Scripts + Docs: `commands/chat/chat.sh` (inject) + `inject.md` — slash commands inject verbatim.** A LIVE injected message that starts with `/` now bypasses both the signature footer (which would land as command arguments) and the long-message file-cap (a file pointer gets read, never run). The command lands clean so the target executes it. Non-command messages are unchanged — long prose still spills to a file, short prose still carries the footer.
