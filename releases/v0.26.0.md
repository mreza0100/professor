# v0.26.0 — 2026-06-12

## Changed

- Mechanics: `/pcm` — System Wiring records the skill-embedded engine pattern: a skill may ship its own Workflow engine as `{skill}/workflow.js`, invoked via `Workflow({scriptPath})`; rr is the reference case. (safe-auto)
- Docs: rr (source-fetched skill) — upstream released v1.3.0: the embedded reference script is replaced by a committed `workflow.js` engine enforcing judge-steered research rounds (research → judge → research — the judge reads ALL findings after every wave and its output becomes the next wave's input), pressure-test saturation (the judge may not stop the first time it believes the goal is answered), and stage-tiered models (opus judgment, sonnet research/verify, haiku decided-retrieval collectors, `sacred: true` lifts every stage to opus). The synthesizer writes the report file from inside the workflow. The blueprint never vendors rr (`templates/skills/sources.json` only), so no rr template ships here. **`update`: skip — informational only.**

#### → For adopters with an installed blueprint:

Re-pull the rr skill from its canonical repo to get the engine: `git pull` in your rr clone, then `cp SKILL.md workflow.js {project}/.claude/skills/rr/`. Fresh installs pick up v1.3.0 automatically through `templates/skills/sources.json`.
