# v0.44.1 — 2026-07-14

## Fixed

- Scripts: statusline template synced to the current live layout — line 2 gains the 🧮 chat-size segment (tokens the next prompt re-sends) and the 💾 prompt-cache-window meter (TTL 5m/1h via `ENABLE_PROMPT_CACHING_1H`, ✓time-left / ✗time-expired off the transcript mtime) with the session in→out breakdown riding it; the stale cache-hit-% and lines-changed segments removed; the 5h/7d rate-limit bars move from line 2 to the LINE-3 money/limits anchor (beside any `segments.d` local modules). v0.44.0 shipped the segments.d extension point but left the surrounding layout one generation behind the live source. `update`: safe-auto.
- Docs: statusline README — three-line layout documented (identity / session metrics / money & limits), narrow-terminal segment dropping and empty-line-3 behavior stated.

### Source

Regenerated from the live source statusline at release time; refresh-map re-baselined.
