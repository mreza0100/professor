---
name: vision-factory
description: Forge, validate, and stress-test a startup vision using Paul Graham's filters. Three modes — CREATE (Socratic grilling → vision narrative), RESEARCH (extract claims → execute research → validation report), STRESS-TEST (10-filter PG rubric → hardened vision). Load on "vision", "create/build/write a vision", "validate/stress-test/pressure-test a vision", "one-pager", "north star", "founding narrative".
---

Read `.claude/skills/vision-factory/SKILL.md` in full — it is the source-of-truth protocol, and read the `references/*.md` it points to as each mode instructs. Follow it verbatim.

## Codex-only execution mapping

- Mode A interview asks one question at a time — surface each question and wait for the founder's answer.
- Mode B research executes via the shared RR skill (or the project's research pipeline) — internal, no intermediate prompt handed to the user.
- Artifacts (`vision-draft.md`, `vision-research.md`, `vision-scorecard.md`) save where the user specifies. No commits.
