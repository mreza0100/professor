---
name: audit:cortex
description: AI/ML pipeline audit — entirely project-specific. Only installed if the project has an AI/ML pipeline subproject.
---

Read `.claude/skills/audit:cortex/SKILL.md` in full — it is the source-of-truth protocol. Follow it verbatim.

## Codex-only execution mapping

- This skill is read-only — no code changes, no commits.
- Wave history lookup uses git log, not file reads.
