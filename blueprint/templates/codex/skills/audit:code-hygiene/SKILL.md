---
name: audit:code-hygiene
description: Code hygiene audit — ghost fields, dead code, deps, arch, types, naming, quality. Domain-hydrated at install time.
---

Read `.claude/commands/audit/code-hygiene.md` in full — it is the source-of-truth protocol. Follow it verbatim.

## Codex-only execution mapping

- This skill is read-only — no code changes, no commits.
- Run only categories applicable to the requested scope.
