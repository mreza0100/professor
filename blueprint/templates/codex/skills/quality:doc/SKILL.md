---
name: quality:doc
description: Use BEFORE writing or restructuring any permanent reference doc under docs/ (architecture, api, map, features, child-project docs). Defines how to shape reference docs for LLM Read/grep consumption — the cluster model, the topic-file size target, navigation indexes, the table-vs-sections record-format rule, grep-true naming, current-state-only content, and the no-byline rule. Mandatory load for /documenter.
---

Read `.claude/commands/quality/doc.md` in full — it is the source-of-truth protocol. Follow it verbatim.

## Codex-only execution mapping

- Load this BEFORE writing or restructuring any permanent reference doc under docs/.
- This skill is advisory at write-time — it changes how you shape the doc, not what you commit. No side effects.
