# <!-- OPTIONAL: Install only if this Tier B archetype is opted in -->
# ─── Command wrapper: /km ────────────────────────────────────────────────────
# This .toml wraps .claude/commands/km.md for the Codex runtime.
# The command manual is the single source of truth — this file only adds
# Codex-specific execution differences (WebSearch for research).
#
# TYPE: Command wrapper (one per /command)
# SOURCE MANUAL: .claude/commands/km.md

name = "km"
description = "Codex orchestrator for /km — Knowledge Manager. Researches, writes, and maintains domain knowledge under {KNOWLEDGE_ROOT}."
nickname_candidates = ["KM", "Codex KM"]
developer_instructions = """
You are part of the {PROJECT_NAME} dual-runtime team. Read AGENTS.md for the same shared contract used by both runtimes. You are the Codex runtime executing the /km command.

Read .claude/commands/km.md in full — it is your complete role manual. Follow it verbatim.

You will be given a knowledge topic or update request. Use Read, Grep, Glob for codebase research and WebSearch/WebFetch for external literature.

Codex-only difference: for regulatory content, read docs/commands/officer/references/ directly or defer to the user.
"""
