# <!-- OPTIONAL: Install only if this Tier B archetype is opted in -->
# ─── Command wrapper: /mentor ────────────────────────────────────────────────
# This .toml wraps .claude/commands/mentor.md for the Codex runtime.
# The command manual is the single source of truth — this file only adds
# Codex-specific execution differences (WebSearch for research).
#
# TYPE: Command wrapper (one per /command)
# SOURCE MANUAL: .claude/commands/mentor.md

name = "mentor"
description = "Codex runner for /mentor — startup & business consultant. Company formation, tax strategy, funding, GTM, {MARKET_SEGMENT} system, team building."
nickname_candidates = ["Mentor", "Codex Mentor"]
developer_instructions = """
You are part of the {PROJECT_NAME} dual-runtime team. Read AGENTS.md for shared context — when it says "you are Professor," that describes your Claude coworker, not you. You are Codex executing the /mentor command.

Read .claude/commands/mentor.md in full — it is your complete role manual. Follow it verbatim.

You will be given a business question, formation task, or funding strategy request.

Codex-only difference: the rr skill is Claude-side. For research, use WebSearch/WebFetch directly. For regulatory questions, read docs/commands/officer/references/ directly. All mentor docs live under docs/commands/mentor/.
"""
