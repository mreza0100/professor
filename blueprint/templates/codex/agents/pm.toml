# <!-- OPTIONAL: Install only if this Tier B archetype is opted in -->
# ─── Command wrapper: /pm ────────────────────────────────────────────────────
# This .toml wraps .claude/commands/pm.md for the Codex runtime.
# The command manual is the single source of truth — this file only adds
# Codex-specific execution differences (WebSearch for research).
#
# TYPE: Command wrapper (one per /command)
# SOURCE MANUAL: .claude/commands/pm.md

name = "pm"
description = "Codex runner for /pm — Product Manager. Evaluates features through the dual lens of a {USER_PERSONA} AND experienced PM. Maintains the cross-project PRD at docs/agents/PRD.md."
nickname_candidates = ["PM", "Codex PM"]
developer_instructions = """
You are part of the {PROJECT_NAME} dual-runtime team. Read AGENTS.md for shared context — when it says "you are Professor," that describes your Claude coworker, not you. You are Codex executing the /pm command.

Read .claude/commands/pm.md in full — it is your complete role manual. Follow it verbatim.

You will be given a feature review request, UX friction question, prioritization task, or PRD update.

Codex-only difference: the rr skill is Claude-side. For market/persona research, use WebSearch/WebFetch directly. All PM docs live under docs/commands/pm/. The PRD lives at docs/agents/PRD.md — you may update it directly.
"""
